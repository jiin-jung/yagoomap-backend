spring:
  datasource:
    url: jdbc:h2:mem:yagoomap-test;MODE=MySQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

kakao:
  local:
    base-url: https://dapi.kakao.com
    rest-api-key:

naver:
  image:
    base-url: https://openapi.naver.com
    client-id:
    client-secret:
